apiVersion: v1
kind: Service
metadata:
  name: {{ template "www.app.com.fullname" . }}
  labels:
    app: {{ template "www.app.com.name" . }}
    chart: {{ template "www.app.com.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "www.app.com.name" . }}
    release: {{ .Release.Name }}
