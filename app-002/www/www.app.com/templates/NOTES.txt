{{ if not (eq .Values.nginx.image.tag "test" ) }}
USE THE APP:
The application is hosted in AWS and behind an Elastic Load Balancer.

Alternatively, if Route 53 DNS has already been configured to redirect to the load balancer, then just navigate to tracker.redgatefoundry.com to see the latest changes.

{{ else }}
USE THE APP:
Get the application URL by running the following:

minikube service {{ .Values.nginx.name }}

This will open the web browser at this url and display the application
{{ end }}
LOGS:
To see the logs as the come through, do the following:

kubectl get pods

kubectl logs -f <pod-name>